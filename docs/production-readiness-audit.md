# Production Readiness Audit

Objective: make AI AssemblyLine fully functional with real APIs and ready for production.

## Current status

This document tracks concrete production gaps and verified evidence. Passing unit tests or a successful build is not treated as production readiness unless it covers the specific requirement.

## Checklist

| Requirement                                              | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Status                                                               |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------- |
| Prisma-backed repository layer                           | Auth sessions, workspace/project ownership, project CRUD/dashboard reads, provider-key storage, project style updates, script upload/version metadata, script-version numbering from persisted Prisma versions, previous script scene/shot superseding, script-version reads for out-of-process workers, generated script-analysis scenes/shots/assets/requirements, scene/shot/asset editor mutations, imported graph reconstruction and import graph mirroring, Asset Bible asset/style lookup for detail edits, uploads, status transitions, merge/split corrections, and ProjectStyle updates, typed Asset Bible detail writes and graph readback, Asset Bible version/reference writes and graph readback, asset-reference worker asset/version inputs, storyboard worker shot/scene/frame/version inputs, storyboard sketch attachment inputs, storyboard frame-version lookup for review updates, storyboard frame/version/review-note writes and graph readback, video worker frame/clip/version inputs, video clip/version writes and graph readback, collaboration invitation/member lookup/assignment/activity writes and graph readback, export bundle writes/listing, job metrics, Prisma graph reads for project/API surfaces, awaited generation job creation/completion/provider-submission transitions, awaited job-event writes before SSE publication, worker job running/polling/completion transitions for analysis/image/video/project jobs without local in-memory job state, and SSE event replay now use Prisma when `REPOSITORY_MODE=prisma` or `NODE_ENV=production`. `repository.prisma.test.ts` forces Prisma mode with a mocked Prisma client, and `prisma validate` passes with a local Postgres URL. | Partially complete                                                   |
| Remove in-memory app state from runtime                  | Direct `getStore()` calls have been removed from the workflow service modules (`scriptAnalysis`, `assetBible`, `storyboard`, `video`, `collaboration`, and `exportImport`). Asset Bible readiness refresh now delegates to repository local/Prisma helpers instead of reading global store state in the service. The in-memory store still exists inside `repository.ts` as the test/development fallback and is still referenced by queue tests that seed local fixtures. Project/API graph reads, storage orphan detection, foundational repository entry points, project style updates, script upload/version metadata, script analysis worker script-version reads, generated scene/shot/asset/requirement records, scene/shot/asset editor mutations, imported graph persistence, typed Asset Bible detail records, Asset Bible status/manual requirement mutations, Asset Bible version/reference records, asset-reference worker inputs, storyboard worker inputs, storyboard sketch attachment inputs, storyboard frame-version review updates, storyboard frame/version/review-note persistence, video clip/version persistence, collaboration persistence, Asset Bible merge/split corrections and style lookup, export bundle records, and job/event records are Prisma-aware.                                                                                                                                                                                                                                                                                                                                                                                                                                            | Partially complete                                                   |
| BullMQ + Redis queues                                    | `submitGenerationJob` submits to BullMQ when Redis mode is enabled; script upload/re-analysis returns pending state in Redis mode and `npm run worker` starts an analysis worker that processes `script_analysis` jobs. Asset reference, storyboard frame, and video clip generation return queued state in Redis mode; image and video workers process `asset_reference`, `storyboard_frame`, and `video_clip` jobs. The media worker now consumes `thumbnail` and `media_convert` jobs through FFmpeg-backed processors, and project export/import return queued state in Redis mode with a project worker for `export` and `import` jobs. Worker startup awaits repeatable Runway provider-poll registration so scheduler failures surface immediately. Worker processors persist failed GenerationJob status, error message, error class, and a project event before rethrowing to BullMQ for retry/dead-letter handling; malformed BullMQ payloads can recover the project from the persisted GenerationJob before emitting that final event. Workers accept optional global or per-queue BullMQ rate limiters from environment variables, and health checks report Redis-backed queue counts plus configured limiters. Tests avoid Redis sockets in test mode.                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Partially complete                                                   |
| Redis-backed SSE                                         | `emitProjectEvent` publishes to the documented `project:{projectId}:events` Redis channel and the SSE subscription listens to the same project channel when Redis mode is enabled. Redis-mode delivery no longer invokes process-local listeners, so production live updates depend on Redis pub/sub instead of same-process shortcuts. Redis publish failures now surface to the awaited job-event path instead of being silently dropped. The subscriber ignores malformed messages and messages from unrelated channels. SSE catch-up replay reads persisted Prisma events in Prisma mode. Mocked Redis tests cover BullMQ submission, Redis publish, Redis-mode local-listener bypass, inline-mode local delivery, publish-failure surfacing, subscribe, channel filtering, and unsubscribe behavior. Route tests cover project-permission enforcement, persisted catch-up replay, and subscription setup; local runtime verification is blocked until Redis is available.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Partially complete                                                   |
| Real OpenAI calls                                        | OpenAI adapter no longer throws for live keys. It calls `/v1/responses` for text/structured output and `/v1/images/generations` for images. Script analysis, Asset Bible reference generation, and storyboard generation now resolve the project workspace's encrypted OpenAI provider key, then fall back to `OPENAI_API_KEY`, then mock/local deterministic mode only outside production. Queued script-analysis jobs now use the same credential resolution before recording provider/model metadata, so live-key jobs are labeled `openai` instead of `local-mock`. Mocked HTTP tests cover payload and error-class mapping.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Partially complete                                                   |
| Real Stability calls                                     | Stability image generation now resolves the project workspace's encrypted `stability` provider key, then falls back to `STABILITY_API_KEY`. With a real key it calls Stable Image Core/Ultra over `https://api.stability.ai/v2beta/stable-image/generate/*` using multipart form data and maps provider HTTP failures to retry classes. `npm run smoke:stability` performs a one-image live smoke call when `STABILITY_API_KEY` is set. Mocked HTTP tests cover the live request shape, error mapping, and smoke helper; no real Stability key has been verified in this environment yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Partially complete                                                   |
| Real Runway calls                                        | Runway video generation now resolves the project workspace's encrypted `runway` provider key, then falls back to `RUNWAYML_API_SECRET`. With a real key it submits Runway video tasks to `https://api.dev.runwayml.com/v1/image_to_video`, stores the provider task ID, and leaves the AssemblyLine job in `provider_submitted` instead of writing mock video bytes. The visible UI, public video generation API route, server-side generation function, and Redis worker path all accept Runway only for shot and scene video generation, avoiding the development-only Kling placeholder in production workflows. The Runway result processor polls task status, downloads successful output URLs, writes the video file, inspects media metadata, persists the ClipVersion, and completes the job. Direct Runway generation and task-status checks both reject missing/mock credentials in production, and mock-backed placeholder provider status checks share the same production guard. In Redis mode, a repeatable video-queue `provider_poll` job finds submitted/polling Runway video jobs from the repository and invokes that processor. Mocked HTTP and queue tests cover task submission, status mapping, error-class mapping, completed output download, Prisma poll-job lookup, repeatable queue registration, non-Runway generation rejection, and missing-key production rejection.                                                                                                                                                                                                                                                                                                                                 | Partially complete                                                   |
| Provider key configuration surface                       | The dashboard provider-key selector and `/api/provider-keys` route now accept only the live-wired providers: OpenAI, Stability, and Runway. Repository validation rejects unsupported provider slugs such as `replicate` with `unsupported_provider`, preventing production users from saving credentials for adapters that are still development placeholders. E2E covers the dashboard provider list.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Passing                                                              |
| Placeholder provider capability labeling                 | Operations metadata can still expose Seedance, Pika, Luma, and ElevenLabs capability snapshots for roadmap planning, but every snapshot is marked `productionReady: false` and `executionMode: "development_mock_only"`. The operations panel labels them as development-only instead of listing them as executable production adapters. Unit and E2E tests cover the metadata and UI label.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Passing                                                              |
| Real provider smoke tests                                | `npm run smoke:providers` runs the OpenAI, Stability, and Runway smoke checks in one release gate. Individual commands remain available as `npm run smoke:openai`, `npm run smoke:stability`, and `npm run smoke:runway`. The smoke commands load `.env`, `.env.production`, `.env.local`, and `.env.production.local` while preserving exported shell overrides, matching production preflight. The OpenAI smoke performs a low-token Responses API structured-output call when `OPENAI_API_KEY` is set. The Stability smoke requests one Stable Image Core image when `STABILITY_API_KEY` is set. The Runway smoke submits a short async video task when `RUNWAYML_API_SECRET` is set and prints the returned provider task id without waiting for final output. Mocked tests cover env-file loading, missing-key failures, and live request shapes; no real provider keys have been verified in this environment yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Blocked                                                              |
| Health checks                                            | `GET /api/health` now actively checks Postgres with `SELECT 1`, Redis with `PING`, and `STORAGE_ROOT` with a mkdir/write/delete probe, returning `503` when any release-critical dependency is unreachable or unwritable. It also reports non-secret `providerEnv` readiness for OpenAI, Stability, and Runway server fallback keys without exposing key values. Production health responses redact raw dependency exception text unless `HEALTH_VERBOSE_ERRORS=1` is set for private diagnostics. `health.test.ts` covers healthy, degraded, provider-env, storage-writability, and production-redacted dependency states.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Passing for mocked dependencies; blocked for local real services     |
| API error capture                                        | Unexpected route errors are captured through `captureError` before returning `500`; production `500` responses redact raw exception messages while captured logs retain operator detail. Expected `AppError`, malformed JSON, and Zod request-validation responses are not reported. `errors.test.ts` covers application errors, invalid JSON, validation errors, unexpected failures, and production redaction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Passing                                                              |
| Project API authorization                                | Project routes require a session and check project permissions at the route layer. Asset Bible and storyboard mutations now enforce permissions per action instead of requiring generation/upload rights for every mutation, so reviewers can approve/reject assets and frames while still being blocked from provider generation, uploads, style changes, and edit corrections. Storyboard frame-version updates/comments and video clip review updates verify the target version belongs to the requested project before persisting changes. Route tests cover reviewer approval and generation denial for Asset Bible/storyboard surfaces, and service tests cover cross-project frame/clip-version rejection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Passing                                                              |
| Local dependency bring-up                                | `compose.yaml` defines PostgreSQL 16 and Redis 7 with health checks and persistent volumes. `npm run services:up`, `services:down`, and `services:logs` wrap Docker Compose for local production-like dependencies. Runtime verification is blocked because Docker is not installed in this environment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Partially complete                                                   |
| Next.js production build conventions                     | The request guard uses `src/proxy.ts` with `export function proxy`, replacing the deprecated `src/middleware.ts` convention. `proxy.test.ts` covers protected-route redirects and authenticated pass-through.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Passing                                                              |
| Production bundle tracing                                | `next.config.ts` excludes `./storage/**` from output file tracing so local media artifacts are not scanned into production route traces during `next build`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Passing                                                              |
| Dependency security audit                                | `package.json` uses an npm override to pin `postcss` to `8.5.14`, replacing the vulnerable `8.4.31` nested under Next.js. `npm run security:audit` wraps `npm audit --audit-level=moderate`, and `npm run preflight:production` runs the same audit as a release gate. `npm audit --audit-level=moderate` now reports zero vulnerabilities and `npm ls postcss` shows Next and Vite both using `8.5.14`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Passing                                                              |
| FFmpeg media metadata and utilities                      | `inspectClip` validates generated media files, records file size, reads duration/resolution/codec through `ffprobe` when available, and falls back to explicit placeholder metadata when FFmpeg tools are unavailable. The Redis media worker can now run `thumbnail` and `media_convert` jobs with progress/completion events when `ffmpeg` is installed. Runtime verification with real FFmpeg is blocked because `ffmpeg` and `ffprobe` are not installed in this environment.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Passing for mocked `ffmpeg`/`ffprobe`; blocked for real local FFmpeg |
| Playwright E2E tests                                     | `e2e/project-workflow.spec.ts` covers sign-in, project creation, script analysis, asset approval, storyboard frame approval, video generation, export bundle UI, and dedicated script, Asset Bible, storyboard, and video route rendering.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Passing for current local workflow                                   |
| Multi-page workflow UI                                   | Dedicated routes now exist for overview, script, Asset Bible, storyboard, and video workflows. E2E checks script, Asset Bible, storyboard, and video route rendering/filtering.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Passing for local workflow                                           |
| Storyboard drawing library                               | Fabric.js is installed and the storyboard page exposes a canvas with draw/select/rectangle/text/clear/save controls. E2E saves rectangle markup through the storyboard API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Passing for local workflow                                           |
| OAuth for app sign-in, OpenAI/ChatGPT, and Google AI Pro | Google/GitHub OAuth sign-in buttons now render on `/signin` when provider credentials are configured. Official OpenAI docs support OAuth for GPT Actions where ChatGPT authenticates to this app's API, not as a general way for this app to spend a user's ChatGPT subscription quota. Google Vertex AI supports Google Cloud authentication such as Application Default Credentials for production; Google sign-in and Google AI Pro subscriptions are not treated as model API quota.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | App OAuth implemented; provider-quota boundaries documented          |
| OAuth app session authorization                          | App session resolution now accepts database-backed Auth.js/NextAuth sessions in addition to the local credentials session cookie. OAuth sessions carry the Auth.js user ID and route authorization reloads that user through the repository before granting app access. `session.test.ts` covers credentials-cookie priority, OAuth/NextAuth fallback, and unauthenticated rejection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Passing                                                              |
| OAuth production preflight                               | `npm run preflight:production` accepts omitted optional Google/GitHub OAuth providers but fails when only a client ID or only a client secret is configured, preventing half-configured OAuth sign-in from reaching production. `productionPreflight.test.ts` covers both omitted and partial OAuth configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Passing                                                              |
| Auth URL production preflight                            | `npm run preflight:production` now validates `NEXTAUTH_URL` as an absolute origin URL and requires HTTPS outside localhost, preventing insecure, malformed, or deep-link callback URLs from reaching production. `productionPreflight.test.ts` covers HTTPS, localhost HTTP, remote HTTP rejection, invalid URL rejection, and path/query rejection.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Passing                                                              |
| Queue/storage/dependency production preflight            | `npm run preflight:production` now verifies `NODE_ENV=production`, verifies that `STORAGE_ROOT` is configured and writable, rejects `QUEUE_MODE=inline` or unknown queue modes for production, rejects explicit `REPOSITORY_MODE=memory`, and validates Postgres/Redis URL schemes before TCP reachability checks. Leaving `QUEUE_MODE` and `REPOSITORY_MODE` unset is accepted because production defaults to Redis queues and Prisma persistence. `productionPreflight.test.ts` covers production runtime mode, writable, missing, and invalid storage roots; redis, unset, and inline queue-mode cases; prisma, unset, and memory repository-mode cases; and invalid Postgres/Redis URL schemes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Passing                                                              |
| Production runtime config mode guards                    | Runtime config validation now rejects `QUEUE_MODE=inline` and `REPOSITORY_MODE=memory` when `NODE_ENV=production`, preventing production startup from using development-only execution modes even if preflight is skipped. `config.test.ts` covers both rejection paths and the accepted `redis`/`prisma` configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Passing                                                              |
| Environment example accuracy                             | `.env.example` now explicitly includes local-only `QUEUE_MODE=inline` and `REPOSITORY_MODE=memory`, while setup/deployment docs classify both as optional runtime-mode overrides and warn that production must leave them unset or set `redis`/`prisma`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Passing                                                              |
| GitHub CI release guard                                  | `.github/workflows/ci.yml` now runs on pull requests and pushes to `main`, covering `npm ci`, Prisma client generation/schema validation, dependency audit, Vitest, lint, production build, Chromium installation, and Playwright E2E. The browser step uses inline queues, memory repository mode, local storage, and mocked/local providers so non-secret gates run without production credentials while config tests remain free to exercise production-mode guards.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Passing                                                              |
| Dependency-audit production preflight                    | `npm run preflight:production` now fails when `npm audit --audit-level=moderate` reports a moderate-or-higher advisory. `npm run security:audit` exposes the focused dependency audit command. `productionPreflight.test.ts` covers success and failure reporting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Passing                                                              |
| Prisma schema release gate                               | `npm run preflight:production` now runs `prisma validate --schema prisma/schema.prisma` through the local Prisma CLI before release, using the configured `DATABASE_URL` or a harmless fallback URL for schema-only validation. `npm run prisma:validate` exposes the same schema check as a focused debugging command. `productionPreflight.test.ts` covers success and failure reporting.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Passing                                                              |
| Prisma migration release gate                            | `npm run preflight:production` now verifies that `prisma/migrations/migration_lock.toml` targets PostgreSQL and that every migration directory contains a non-empty `migration.sql`. `npm run prisma:migrate:deploy` provides the production migration apply command. `productionPreflight.test.ts` covers valid migrations, empty migration SQL, and wrong-provider locks.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Passing                                                              |
| Production preflight env-file loading                    | The preflight command now loads `.env`, `.env.production`, `.env.local`, and `.env.production.local` before checking release gates, while preserving exported shell-variable overrides. `productionPreflight.test.ts` covers env-file precedence, quotes, comments, `export` prefixes, and shell overrides. Runtime verification with a temporary ignored `.env.production.local` reduced preflight failures to the expected missing local FFmpeg/Postgres/Redis blockers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Passing                                                              |
| Production secret fallback guard                         | Runtime config parsing and `npm run preflight:production` reject the checked-in development `NEXTAUTH_SECRET` and `ENCRYPTION_KEY` fallback values in production even though they have valid length/shape. `config.test.ts` and `productionPreflight.test.ts` cover these cases.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Passing                                                              |
| Provider credential placeholder guard                    | Provider credential classification rejects `mock`, generic placeholders, and checked-in example provider keys such as `sk-live-test`, `sk-stability-live-test`, and `key_runway_live` as non-live credentials. Runtime provider-key saves, production preflight, smoke helpers, and health/provider readiness use the same live-key guard.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Passing                                                              |
| Production runtime verification                          | Postgres and Redis are not currently reachable on local default ports.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Blocked                                                              |

## Latest verification

- Script revision superseding now batches previous-version scene and shot status updates into one Prisma transaction, preventing partial superseded state after late database failures.
- Script revision upload persistence now batches new script creation when needed, prior-version deactivation, and new active ScriptVersion creation into one Prisma transaction, preventing partial active-version state after late database failures.
- Frame and clip approval persistence now batches previous-version superseding and selected-version approval into one Prisma transaction, preventing partial approval state after late database failures.
- Generated video clip persistence now batches the Prisma clip upsert, clip-version insert, and generation-job completion into one transaction, preventing partial generated-clip state after late database failures.
- Generated storyboard frame persistence now batches the Prisma frame upsert, frame-version insert, shot status update, and generation-job completion into one transaction, preventing partial generated-frame state after late database failures.
- Prisma project creation now verifies project media-directory setup before writing the project row, preventing storage setup failures from leaving production projects without backing media directories.
- Asset Bible merge persistence now batches scene/shot requirement reassignment and both source/target Prisma asset updates into one transaction, preventing a failed merge from leaving requirements and asset lifecycle state out of sync.
- Generated script analysis graph persistence now batches the regenerated Prisma scene, shot, asset, and requirement writes into one transaction, preventing a late graph-write failure from leaving a partially replaced production dependency graph.
- Project bundle import graph persistence now batches restored Prisma records into a single transaction and restores historical generation jobs before dependent media/version rows, preventing late storyboard/video/review write failures from leaving partially imported production graphs.
- Workspace provider-key replacement now runs the Prisma delete/create pair inside a single transaction, preventing a failed credential replacement from deleting the previously working production key.
- Project storage path helpers now reject path separators and traversal characters before constructing media directories, keeping cleanup, thumbnail, export, and media-write paths under `STORAGE_ROOT/projects/{projectIdWithoutDashes}`.
- Project operations cleanup actions now rebuild their operations payload after mutating storage, so operator-facing orphan and thumbnail lists reflect the state after cleanup instead of the stale pre-action snapshot.
- Production config and preflight now reject the known development fallback `NEXTAUTH_SECRET` and `ENCRYPTION_KEY` values, preventing copied local defaults from satisfying production shape checks.
- Provider key safety now rejects known placeholder/test values as non-live credentials, so checked-in smoke-test examples cannot satisfy production preflight or production provider-key saves.
- Production preflight now requires `NODE_ENV=production`, matching the runtime branches that enable production repository mode, Redis queue defaults, secure auth cookies, production error redaction, and provider mock guards.
- Production preflight now loads standard ignored env files before checking release gates, while exported shell variables remain authoritative for one-off overrides.
- Application session resolution now falls back to database-backed Auth.js sessions, so Google/GitHub OAuth sign-in can authorize normal app pages and API routes instead of only rendering sign-in buttons.
- Placeholder provider capability snapshots now carry explicit non-production metadata and the operations panel labels them as development-only, keeping roadmap/provider-planning metadata separate from live executable providers.
- Provider smoke commands now load the same standard ignored env files as production preflight, so release checks can be run from `.env.production.local` without manually exporting every provider key.
- Health checks now probe `STORAGE_ROOT` writability, so deployment monitoring degrades when media storage is misconfigured instead of reporting only the configured path.
- Production preflight now validates the Prisma schema through the local Prisma CLI, and `npm run prisma:validate` is available for focused schema checks.
- Production preflight now validates checked-in Prisma migration files and `npm run prisma:migrate:deploy` is available for production migration application.
- Production preflight now runs the dependency security audit, and `npm run security:audit` is available as a focused release check.
- Production runtime config now rejects inline queue mode and explicit memory repository mode, matching the preflight production-mode gates.
- The environment example and setup docs now show the intended local-only runtime modes and distinguish them from production defaults.
- Project bundle import now restores remapped collaboration metadata, historical job records, and job events. Imported assignments and activity actors point to the importing user, invitation token hashes are regenerated, and active historical jobs are converted to `canceled` so imports do not imply resumable queue work.
- Project bundle import now rejects malformed assignment records whose target type does not match exactly one scene, shot, or asset target ID.
- Video review updates now read individual clip versions through the repository layer before persisting approval/superseded state, so Prisma mode does not depend on the script-analysis graph or in-memory clip-version search for that path.
- Script uploads now ask the repository for the next persisted script-version number and mark previous-version scenes/shots superseded through Prisma, so production uploads no longer depend on local in-memory script-version mirrors for those transitions.
- Asset status downgrades now mark dependent storyboard frame versions stale from the repository graph and persist the stale review state, rather than relying only on the in-memory graph.
- Asset reference uploads now create asset versions from repository asset and version history, preserving local mirroring only for development/test compatibility.
- Requirement editor add/remove operations now read scene-asset links through repository helpers before duplicate checks and deletion, so Prisma mode no longer depends on local requirement collections for those edits.
- Storyboard frame re-approval now persists stale state to dependent video clip versions through the repository when upstream approved frames are superseded.
- Video generation now locates existing shot/scene clips through repository helpers before creating a new clip version, so clip reuse in Prisma mode does not depend on local video clip state.
- Export/import jobs now await repository job completion updates, and import returns the restored graph through the repository read path after persistence.
- The dashboard provider-key form now starts empty, uses password entry, and blocks blank/mock-prefilled saves so production setup requires an intentional provider key entry.
- OpenAI generation credential resolution now allows mock fallback only outside production; production requires an encrypted workspace OpenAI key or `OPENAI_API_KEY` and fails with `provider_key_missing` when neither is configured or the resolved key is the literal `mock`.
- Provider-key saving now rejects a literal `mock` key for every provider in production before encryption/storage.
- Stability image generation now has a live HTTP path for Stable Image Core/Ultra when a workspace Stability key or `STABILITY_API_KEY` is configured.
- `npm run smoke:stability` now provides a release-time live Stability API check when `STABILITY_API_KEY` is available.
- Stability successful HTTP responses now must include non-empty image bytes; empty image bodies fail as fatal provider errors instead of being stored as generated media.
- Mock-backed placeholder providers now fail with `provider_not_configured` in production instead of silently returning fake Runway, Kling, Seedance, Pika, Luma, or ElevenLabs outputs.
- Direct OpenAI adapter mock-mode calls now fail with `provider_not_configured` in production, covering callers that bypass project credential resolution.
- OpenAI successful HTTP responses now must include output text for Responses API calls and non-empty base64 image data for image calls; malformed success payloads fail as fatal provider errors instead of being stringified or accepted as empty image output.
- Runway video generation now has a live task-submission path when a workspace Runway key or `RUNWAYML_API_SECRET` is configured, plus a result processor that polls completed tasks and downloads output media. Redis workers now register a repeatable video provider poll job that automatically invokes that processor for submitted/polling Runway jobs.
- Video generation now fails the GenerationJob with `provider_output_missing` when a provider response contains neither video bytes nor an async provider job id, instead of writing placeholder `mock-video` bytes.
- Runway task submission now rejects successful HTTP responses that do not include a task ID, preventing `providerJobId: "undefined"` from entering polling state.
- Direct Runway and mock-backed placeholder adapter task-status checks now share the production mock-provider guard, so missing/mock credentials cannot return fake completed poll status in production.
- Redis workers now register the `media` queue, and `thumbnail`/`media_convert` jobs invoke FFmpeg-backed processors that emit progress and completion events. Real local media utility verification is still blocked until `ffmpeg` and `ffprobe` are installed.
- Storage usage scans now treat missing folders as empty but surface permission/read errors instead of reporting zero usage for an unreadable project tree.
- Asset reference and storyboard sketch uploads now reject zero-byte files with `empty_media` before creating versions, frames, or media files.
- Asset reference uploads now persist uploaded asset versions and references only after the media file write succeeds, preventing orphaned AssetVersion records when storage fails mid-upload.
- Asset reference and storyboard sketch multipart routes now return `missing_upload_file` client errors when the file part is absent instead of surfacing plain unexpected server errors.
- Script multipart uploads now return `missing_upload_file` client errors when the file part is absent instead of surfacing plain unexpected server errors.
- Asset reference and storyboard sketch multipart routes now validate required target IDs at the route boundary, returning `missing_upload_target` for absent or malformed `assetId`/`shotId` before service calls.
- Zod request-validation failures now return `400 validation_error` with issue paths and are not captured as unexpected route errors.
- Malformed JSON request bodies now return `400 invalid_json` and are not captured as unexpected route errors.
- Production API `500` responses now redact raw exception messages while still capturing the original error for operators.
- Redis worker startup now awaits repeatable Runway provider-poll job registration and rejects startup if BullMQ cannot schedule it.
- Redis workers now persist failed GenerationJob state and a final project event when a processor throws, while still rethrowing to BullMQ so queue retries and dead-letter handling remain active.
- Redis workers now use the persisted GenerationJob project when a malformed BullMQ payload is missing `projectId`, so validation failures still produce terminal job state and a project event when the job record exists.
- Worker failures now persist retry counts from BullMQ attempts, and project operations metrics report total retries, retried job count, and retries by job type.
- Worker failure persistence now surfaces GenerationJob lookup failures instead of treating production database read errors as absent job state.
- Queue health now reports per-queue `healthError` values when BullMQ count/failure reads fail, and the operations panel displays those errors instead of presenting fallback zeros as healthy data.
- `GET /api/health/workers` now returns `503` with `status: "degraded"` when any queue reports a health error.
- The operations panel now surfaces `/api/projects/{projectId}/operations` load failures in the UI instead of silently hiding storage, queue, and adapter diagnostics.
- Asset Bible API mutations now enforce route permissions per action, allowing reviewer approval/rejection without upload-reference rights while still denying reviewer provider-generation requests.
- Storyboard API mutations now enforce route permissions per action, allowing reviewer frame approval/rejection and comments without storyboard-generation rights while still denying reviewer provider-generation requests.
- Storyboard frame generation now validates keyframe indexes in both inline and Redis worker paths before frame creation, rejecting indexes outside 0-8 with `bad_keyframe`.
- Video clip status updates now reject cross-project clip-version IDs before persistence, preventing a reviewer on one project from mutating another project's clip by ID.
- Storyboard frame status/comment updates now reject cross-project frame-version IDs before persistence, preventing a reviewer on one project from mutating another project's frame by ID.
- Video generation now validates target shape and ownership before provider job creation: shot mode requires exactly one route-project `shotId`, scene mode requires exactly one route-project `sceneId`, and invalid combinations fail with `invalid_video_target`.
- Script-analysis scene, shot, detected-asset, and scene-asset requirement mutations now verify that every target ID belongs to the requested project before persistence, preventing project members from mutating another project's breakdown records by ID.
- Asset Bible detail, status, merge, and split mutations now verify route-project ownership for every referenced asset ID before persistence, preventing cross-project asset edits after route-level authorization.
- Collaboration assignments now require a project-member assignee, exactly one target ID matching the declared target type, and route-project ownership for scene/shot/asset targets before persistence.
- Project import now resolves manifest paths under `STORAGE_ROOT` and requires the `.assemblyline-bundle.json` suffix before reading, preventing operations import from probing arbitrary filesystem paths.
- Project import now validates readable bundle manifests for required project metadata and graph collections before creating imported records, returning `invalid_import_bundle` for malformed JSON shape.
- Project import now validates bundle graph references across scenes, shots, assets, asset versions, frame versions, clip versions, requirements, and review notes before creating imported records, returning `invalid_import_bundle` for broken links.
- Project import now rejects duplicate graph IDs within imported collections before ID remapping, preventing ambiguous scene/shot/asset/frame/clip reconstruction.
- Queue health now reports delayed/completed counts and up to 10 recent failed BullMQ jobs per Redis-backed queue; the operations panel surfaces queue health and recent failure summaries.
- Redis workers now support optional BullMQ worker rate limiters through global `QUEUE_RATE_LIMIT_MAX`/`QUEUE_RATE_LIMIT_DURATION_MS` settings or per-queue overrides such as `IMAGE_QUEUE_RATE_LIMIT_MAX`/`IMAGE_QUEUE_RATE_LIMIT_DURATION_MS`; queue health and the operations panel expose configured limits.
- Job lifecycle repository calls now await Prisma `GenerationJob` and `JobEvent` writes before queue submission, completion returns, or SSE publication, so production failures surface instead of creating orphaned queue work or unpublished persisted events.
- Redis project-event publication is now awaited by the repository event path, so production Redis pub/sub failures surface instead of silently dropping live SSE delivery after a job event is stored.
- Redis project-event subscription failures now emit `stream_error` SSE events instead of being silently swallowed during live event setup.
- Runway completed-task output downloads now reject empty response bodies as `provider_output_missing` instead of creating zero-byte clip versions.
- Runway repeatable provider-poll processing now persists per-video GenerationJob failures and emits final project events instead of leaving the original video job stuck after the scheduler job catches a provider result error.
- Runway repeatable provider-poll processing now leaves retriable rate-limit, timeout, and provider `5xx` failures in `polling` state so the next scheduled poll can retry instead of prematurely terminal-failing the video job.
- `npm run smoke:providers` now runs OpenAI, Stability, and Runway live smoke checks as a single release gate while preserving the individual provider smoke commands for debugging.
- `npm run preflight:production` now verifies `STORAGE_ROOT` with a real mkdir/write/delete probe and rejects production `QUEUE_MODE=inline`; unset `QUEUE_MODE` remains valid because production defaults to Redis.
- `npm run preflight:production` now rejects non-Postgres `DATABASE_URL` schemes and non-Redis `REDIS_URL` schemes before TCP reachability checks.
- `npm run preflight:production` now validates `NEXTAUTH_URL` and rejects malformed URLs, non-HTTPS remote URLs, and URLs with a path/query/fragment; localhost HTTP origins remain allowed for local verification.
- Asset Bible approval and merge operations now refresh scene/shot readiness through repository helpers instead of reading in-memory store state directly from the service module.
- Video generation workers and server-side generation calls now reject non-Runway provider slugs, closing the last mock-backed Kling execution path while leaving placeholder capabilities visible only as non-executable metadata.
- Workspace provider-key lookup and decryption failures now surface during OpenAI, Stability, and Runway credential resolution instead of falling back to server environment keys; fallback is reserved for genuinely absent workspace keys.
- OpenAI, Stability, and Runway adapters now trim direct API key inputs before mock/live classification and Authorization header construction, preventing whitespace-padded `mock` values from bypassing the production mock-provider guard.
- Provider-key guards, preflight checks, health reporting, smoke helpers, and live adapters now treat `mock` case-insensitively, so values such as `MOCK` or whitespace-padded mock values cannot be mistaken for live production credentials.
- Runtime config parsing now rejects `ENCRYPTION_KEY` values unless they decode to exactly 32 bytes, matching the production preflight and AES-256-GCM provider-key encryption requirement.
- `npm run rotate-keys` now re-encrypts stored provider keys from `ENCRYPTION_KEY_OLD` to `ENCRYPTION_KEY`, with tests covering old-key decrypt/new-key encrypt and invalid rotation-key rejection.
- Runtime config parsing now rejects production `NEXTAUTH_URL` values that are not origin-only HTTPS URLs outside localhost, matching the production preflight callback URL gate.
- Queued script-analysis job metadata now resolves OpenAI credentials before job creation, labeling live-key analysis as `openai` and reserving `local-mock` for deterministic local fallback.
- Queued script upload and re-analysis responses now read the pending graph through the async repository graph helper, so Redis-mode production responses use Prisma readback instead of the synchronous local in-memory graph helper.
- Storyboard frame, frame-version, review-note, shot-status, and related generation-job persistence now surface Prisma write failures instead of silently continuing after a missed production database write.
- Video clip, clip-version, superseded-version, and related generation-job persistence now surface Prisma write failures instead of silently continuing after a missed production database write.
- Collaboration invitation, project-member, assignment, and activity-event persistence now surface Prisma write failures instead of silently continuing after a missed production database write.
- Asset Bible asset-version and asset-reference persistence now surface Prisma write failures instead of silently continuing after a missed production database write.
- Export bundle persistence now surfaces Prisma write failures instead of silently continuing after a manifest is written without a matching production database record.
- Asset Bible media writes now compose runtime storage paths through the storage helper instead of direct dynamic `path.join` calls, removing the Turbopack broad file-pattern warning from `next build`.
- Script analysis status updates now surface Prisma write failures instead of silently continuing after a missed production database write.
- Creature and prop Asset Bible detail persistence now surfaces Prisma write failures instead of silently continuing after a missed production database write.
- Imported storyboard/video graph persistence now surfaces Prisma write failures instead of silently continuing after missed imported frame, clip, or requirement records.
- Worker running-state updates now surface Prisma write failures instead of letting a worker continue after failing to mark a production job running or polling.
- Asset Bible merge requirement reassignment now surfaces Prisma write failures instead of silently continuing after missed scene or shot requirement updates.
- Project update writes now distinguish Prisma not-found records from real write failures, surfacing production database errors instead of misreporting every failure as `not_found`.
- Scene and shot readiness refreshes now surface Prisma write failures instead of silently continuing after missed production status updates.
- Export bundle and generation-job reads now surface Prisma failures instead of falling back to stale in-memory state during production-mode database outages.
- Foundational repository lookups for members, scenes, scripts, shots, assets, storyboard/video records, requirements, and invitations now surface Prisma failures instead of treating production database read errors as missing in-memory data.
- Foundational project-member, scene, script-version, shot, asset, frame-version, clip-version, video-clip, scene-requirement, invitation, and generation-job lookups now prefer Prisma records whenever Prisma mode is enabled, so stale local process mirrors cannot override production database state.
- GenerationJob lifecycle writes for running, provider-submitted, and terminal states now return Prisma-updated job records in Prisma mode while only syncing the local mirror as a development compatibility shadow.
- Script-version analysis status writes now return Prisma-updated records in Prisma mode while only syncing the local mirror as a development compatibility shadow.
- Project deletes now distinguish Prisma not-found records from real write failures, surfacing production database errors instead of misreporting every delete failure as `not_found`.
- `npm run preflight:production` now provides a release gate for required env vars, secret/key lengths, optional OAuth pair consistency, live OpenAI/Stability/Runway credentials, FFmpeg/ffprobe availability, and Postgres/Redis TCP reachability.
- Script analysis now uses the OpenAI structured-output adapter for scene, shot, and asset passes when real credentials are configured; deterministic parsing remains available only for local development/tests without provider credentials.
- `npm test`: passing, 36 files and 176 tests.
- `npm run lint`: passing.
- `npm run build`: passing.
- `npm audit --audit-level=moderate`: passing, zero vulnerabilities.
- `npm ls postcss`: Next.js and Vite both resolve to `postcss@8.5.14`.
- `prisma validate`: passing when `DATABASE_URL` is set for schema validation.
- `npm run test:e2e`: passing, 1 Chromium workflow test.
- `QUEUE_MODE=inline npm run worker`: exits cleanly with Redis disabled message.
- `npm run preflight:production`: fails in this environment with explicit blockers: `NODE_ENV` is not set to production, required production env vars are missing, `NEXTAUTH_URL` is missing/invalid, secret/key shape and fallback-value checks fail, `STORAGE_ROOT` is missing, OpenAI/Stability/Runway keys are missing or placeholder values, FFmpeg/ffprobe are missing, and Postgres/Redis URLs are missing. `QUEUE_MODE` passes because it is unset and production defaults to Redis.
- `GET /api/health` against the live local dev server returns `503` with `status: "degraded"` and local dependency diagnostics. The current local server process still lacks `DATABASE_URL`, cannot reach Redis, and reports OpenAI/Stability/Runway fallback keys as unconfigured. Production responses redact raw dependency exception details by default.
- Docker preflight: `docker --version` and `docker compose version` fail because Docker is not installed in this environment.
- Local Postgres TCP check: failed on `127.0.0.1:5432`.
- Local Redis TCP check: failed on `127.0.0.1:6379`.
- Real OpenAI smoke-test preflight: `OPENAI_API_KEY` is not set in this environment.
